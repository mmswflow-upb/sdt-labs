server:
  port: ${SERVER_PORT:8080}

spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/gradingdb}
    username: ${SPRING_DATASOURCE_USERNAME:grading}
    password: ${SPRING_DATASOURCE_PASSWORD:grading}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
