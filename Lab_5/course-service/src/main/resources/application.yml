server:
  port: ${SERVER_PORT:8080}

spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/coursedb}
    username: ${SPRING_DATASOURCE_USERNAME:course}
    password: ${SPRING_DATASOURCE_PASSWORD:course}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
